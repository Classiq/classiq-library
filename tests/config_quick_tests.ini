[pytest]
testpaths =
    tests

# directories to ignore
norecursedirs =
    tests/notebooks
    tests/qmods

addopts = --color=yes --log-cli-level=INFO

#
# Common settings
#
python_files = test_*.py

filterwarnings =
  error
