[pytest]
testpaths =
    tests/qmods
    tests/notebooks
    tests/test_links

addopts = --color=yes --log-cli-level=INFO --durations=0 --durations-min=1

#
# Common settings
#
python_files = test_*.py

filterwarnings =
  error
